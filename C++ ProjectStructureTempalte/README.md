# C++ Project Template

This is a basic template for starting new C++ tutorials or projects.

## Structure

- `include/` - Header files
- `src/` - Source files
- `docs/` - Documentation
- `build.sh` - Build script
- `run.sh` - Run script
- `CMakeLists.txt` - CMake build configuration
